/**
 * 管理画面用のPAT vault（GitHubトークンを管理パスワードで暗号化したもの）
 *
 * - 初期は null。
 * - 管理画面でPATを保存すると、ここに暗号化されたデータが書き込まれます。
 * - 他のPCで管理画面にログインすると、このファイルから自動でPATが復元されます。
 *
 * 暗号化方式：PBKDF2(SHA-256, 100,000回) → AES-GCM 256bit
 * 復号には管理画面のパスワードが必要です。
 */

const ADMIN_VAULT = null;
